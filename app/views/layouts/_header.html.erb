<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/uploads">Extra Space</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav pull-right">
                <% if current_user %>
                <li style="margin-top:25px;margin-right:5px;", class="text-muted">
                    Welcome,
                    <%= current_user.first_name %>
                </li>
                <li>
                    <div class="dropdown">

                        <%= image_tag avatar_url(current_user), class: "header-avatar", "data-toggle": "dropdown", "aria-haspopup": "true", "aria-expanded": "true"%>


                        <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
                            <li>
                                <%= link_to "Edit My Details", edit_user_registration_path %>

                            </li>
                            <li role="separator" class="divider"></li>
                            <li>
                                <%= link_to "Log out", destroy_user_session_path, :method=> :delete %>
                            </li>
                        </ul>
                    </div>
                </li>
                <% end %>
                <%= form_tag(uploads_path, method: 'get', class: 'navbar-form navbar-right') do %>
                <%= search_field_tag 'search', nil, class: 'form-control', placeholder: 'Search Uploads'  %>
                <%= submit_tag "Submit", class: 'btn btn-default', :name => nil %>
                <% end %>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>
